"""Kolay IK Secure RAG — Corporate Memory (Layer 2).

Install optional dependencies:
    uv pip install -e "[rag]"

Environment variables:
    QDRANT_URL          Remote Qdrant cluster URL (default: local path storage)
    QDRANT_API_KEY      API key for remote Qdrant cluster
    QDRANT_LOCAL_PATH   Local Qdrant data directory (default: .qdrant_data)
    FASTEMBED_MODEL     Embedding model (default: intfloat/multilingual-e5-small)
"""
